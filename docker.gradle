import com.bmuschko.gradle.docker.tasks.image.*


buildscript {
    repositories {
        gradlePluginPortal()
    }
    dependencies {
        classpath 'com.bmuschko:gradle-docker-plugin:9.4.0'
   }
}
